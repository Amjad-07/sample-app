version: "3.8"

services:
  backend:
    build: ./backend
    container_name: sample-backend
    ports:
      - "8080:8080"

  frontend:
    build: ./frontend
    container_name: sample-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
